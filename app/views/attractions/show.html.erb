<h3><%= @attraction.name %></h3>
<!-- < button_to "Go on this ride", Ride.new %> -->
<button><%= link_to "Edit Attraction", edit_attraction_path(@attraction) %></button>
<%= form_for Ride.new do |r| %>
	<%= r.hidden_field :attraction_id, value: params[:id] %>
	<%= r.submit "Go on this ride" %>
<% end %>
